<!-- 0379c26a-af81-4c65-a0a5-ec5e9aca2a19 0ada9ae1-75c0-41fd-b967-ae14d4779bed -->
# 服务器选项和实现统一计划

## 目标

根据用户选择（1a, 2b, 3b）：
1. 合并 ServerOptions 和 HTTPServerOptions，保留 ServerOptions，废弃 HTTPServerOptions
2. 统一 HTTP 服务器实现，使用 HTTPOptions（common/server/http/options.go）
3. 统一 gRPC 服务器实现，使用 GRPCOptions（common/server/grpc/options.go）

## 阶段 1: 合并 ServerOptions 和 HTTPServerOptions ✅

### 1.1 增强 ServerOptions ✅
- 添加 `Network` 字段（从 HTTPServerOptions）
- 添加 `MaxHeaderBytes` 字段（从 HTTPServerOptions）
- 更新 `NewServerOptions()` 默认值
- 更新 `Validate()` 方法
- 更新 `Complete()` 方法
- 更新 `AddFlags()` 方法
- 添加 `GetAddr()` 方法
- 添加 `WithNetwork()` 和 `WithMaxHeaderBytes()` 函数

### 1.2 废弃 HTTPServerOptions ✅
- 在 HTTPServerOptions 顶部添加 Deprecated 注释
- 提供迁移指南

## 阶段 2: 统一 HTTP 服务器实现

### 2.1 更新 common/server/http 实现
- 确保 HTTPOptions 使用 ServerOptions
- 更新 Gin 服务器实现以支持新字段（Network, MaxHeaderBytes）
- 废弃旧的 Gin 实现（如果有）

### 2.2 更新 pkg/initializers/http_server.go
- 确保使用 HTTPOptions 实现
- 更新配置传递方式

### 2.3 更新各服务的 HTTP 初始化器
- cluster: internal/cluster/initializers/http_server.go
- gateway: internal/gateway/initializers/http_server.go
- monitor: internal/monitor/initializers/http_server.go
- reasoning: internal/reasoning/initializers/http.go
- orchestrator: internal/orchestrator/initializers/http.go
- auth: internal/auth/initializers/server.go
- agent-manager: internal/agent-manager/initializers/servers.go

## 阶段 3: 统一 gRPC 服务器实现

### 3.1 更新 common/server/grpc 实现
- 确保 GRPCOptions 是标准实现
- 废弃 StandardGRPCServer（如果与 GRPCOptions 冲突）

### 3.2 更新 pkg/initializers/grpc_server.go
- 确保使用 GRPCOptions 实现
- 更新配置传递方式

### 3.3 更新各服务的 gRPC 初始化器
- orchestrator: internal/orchestrator/initializers/grpc.go
- reasoning: internal/reasoning/initializers/grpc.go
- agent-manager: internal/agent-manager/initializers/servers.go

## 阶段 4: 验证和文档

### 4.1 编译验证
- 编译所有服务
- 修复任何编译错误

### 4.2 创建迁移文档
- 记录所有变更
- 提供迁移示例
- 更新相关文档


### To-dos

- [ ] 添加 Google Wire 依赖和工具，创建通用组件注册辅助包
- [ ] 在 cluster 服务实施 Wire 依赖注入（验证方案）
- [ ] 推广 Wire 到简单服务（monitor, gateway, collect-agent）
- [ ] 实施 reasoning 服务 Wire 依赖注入
- [ ] 实施 orchestrator 服务 Wire 依赖注入（复杂依赖链）
- [ ] 合并 ServerOptions 和 HTTPServerOptions
- [ ] 统一 HTTP 服务器实现使用 HTTPOptions
- [ ] 统一 gRPC 服务器实现使用 GRPCOptions
- [ ] 验证编译并创建迁移文档