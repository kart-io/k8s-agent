version: '3.8'

services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: k8s-agent-mysql
    environment:
      MYSQL_ROOT_PASSWORD: mysql
      MYSQL_USER: mysql
      MYSQL_PASSWORD: mysql
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./scripts/init-databases.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - k8s-agent-network
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-pmysql"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: k8s-agent-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - k8s-agent-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NATS 消息队列
  nats:
    image: nats:2.9-alpine
    container_name: k8s-agent-nats
    ports:
      - "4222:4222"
      - "8222:8222"
    command: "--http_port 8222"
    networks:
      - k8s-agent-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8222/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 认证服务
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    container_name: k8s-agent-auth
    ports:
      - "8090:8090"
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=mysql
      - DB_PASSWORD=mysql
      - DB_NAME=k8s_agent_auth
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - k8s-agent-network
    restart: unless-stopped

  # 监控服务
  monitor-service:
    build:
      context: ./monitor-service
      dockerfile: Dockerfile
    container_name: k8s-agent-monitor
    ports:
      - "8081:8081"
      - "9091:9091"
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=mysql
      - DB_PASSWORD=mysql
      - DB_NAME=monitor_db
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - k8s-agent-network
    restart: unless-stopped

  # 集群服务
  cluster-service:
    build:
      context: ./cluster-service
      dockerfile: Dockerfile
    container_name: k8s-agent-cluster
    ports:
      - "8082:8082"
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=mysql
      - DB_PASSWORD=mysql
      - DB_NAME=cluster_db
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - k8s-agent-network
    restart: unless-stopped
    volumes:
      - ~/.kube:/root/.kube:ro

  # API 网关
  gateway-service:
    build:
      context: ./gateway-service
      dockerfile: Dockerfile
    container_name: k8s-agent-gateway
    ports:
      - "8080:8080"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - AUTH_SERVICE_URL=http://auth-service:8090
      - MONITOR_SERVICE_URL=http://monitor-service:8081
      - CLUSTER_SERVICE_URL=http://cluster-service:8082
      - AGENT_MANAGER_URL=http://agent-manager:8080
    depends_on:
      - redis
      - auth-service
      - monitor-service
      - cluster-service
    networks:
      - k8s-agent-network
    restart: unless-stopped

  # 前端 UI
  agent-manager-ui:
    build:
      context: ./agent-manager-ui
      dockerfile: Dockerfile
    container_name: k8s-agent-ui
    ports:
      - "3000:80"
    depends_on:
      - gateway-service
    networks:
      - k8s-agent-network
    restart: unless-stopped

volumes:
  mysql_data:
  redis_data:

networks:
  k8s-agent-network:
    driver: bridge
