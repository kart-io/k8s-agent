# Aetherius Collect Agent - Development Configuration

# Cluster identification (leave empty for auto-detection)
cluster_id: ""
cluster_name: "Development Cluster"

# NATS local endpoint
central_endpoint: "nats://localhost:4222"

# Connection settings (faster for development)
reconnect_delay: 2s
max_retries: 5

# Heartbeat and metrics intervals (faster for testing)
heartbeat_interval: 15s
metrics_interval: 30s

# Buffer settings (smaller for development)
buffer_size: 100

# Feature flags
enable_metrics: true
enable_events: true

# Health check port (different from agent-manager's 8080)
health_port: 8082

# Logging (info level for development - use "debug" for more details)
log_level: "debug"
