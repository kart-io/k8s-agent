apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aetherius

resources:
- deployment.yaml
- service.yaml
- configmap.yaml

labels:
- pairs:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: aetherius

# Secret will be created in overlays
