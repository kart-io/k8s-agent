# buf.yaml - Unified protobuf configuration for k8s-agent
version: v2
modules:
  - path: .
    name: buf.build/kart-io/k8s-agent
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
