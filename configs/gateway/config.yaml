# Gateway Service 配置

server:
  host: "0.0.0.0"
  port: 8080
  mode: debug  # debug, release, test
  read_timeout: 30s
  write_timeout: 30s

# 日志配置
log:
  level: info
  file: ./logs/gateway.log

# Redis 配置 (用于限流、缓存)
redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10

# JWT 配置
jwt:
  secret: "your-gateway-secret-key"  # 生产环境必须修改
  expires_hours: 24

# 限流配置
rate_limit:
  enabled: true
  requests_per_second: 100
  burst: 200

# CORS 配置
cors:
  enabled: true
  allow_origins:
    - "http://localhost:3000"
    - "http://localhost:5173"
  allow_methods:
    - GET
    - POST
    - PUT
    - DELETE
    - PATCH
    - OPTIONS
  allow_headers:
    - Origin
    - Content-Type
    - Authorization
  expose_headers:
    - Content-Length
  allow_credentials: true
  max_age: 43200

# 后端服务配置
services:
  # 认证服务
  auth:
    name: auth-service
    url: http://localhost:8090
    timeout: 30s
    retry: 3
    health_check: /health

  # Agent 管理服务
  agent_manager:
    name: agent-manager
    url: http://localhost:8081
    timeout: 30s
    retry: 3
    health_check: /health

  # 编排服务
  orchestrator:
    name: orchestrator-service
    url: http://localhost:8082
    timeout: 30s
    retry: 3
    health_check: /health

  # 推理服务
  reasoning:
    name: reasoning-service
    url: http://localhost:8083
    timeout: 30s
    retry: 3
    health_check: /health

# 路由配置
routes: []
# Note: Routes are defined in code in cmd/gateway/app/container.go
# This configuration section is kept for future custom route overrides

# 健康检查配置
health_check:
  enabled: true
  interval: 30s
  timeout: 5s

# 监控配置
metrics:
  enabled: true
  path: /metrics
